# Local-only credentials matching compose.yaml. Replace every value outside local
# development and never commit a populated .env file.
DATABASE_URL=postgresql+psycopg://devstash:devstash-local-only@127.0.0.1:5432/devstash
